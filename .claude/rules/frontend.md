---
paths:
  - "src/**"
---

# Frontend (src/)

## Core Files

- **App.tsx** — Root React component: owns app-wide state, hook composition, playback/stream-resolver wiring, context menu, sidebar, and the view render tree. Views toggled via `View` union type: `"home" | "search" | "artists" | "albums" | "tags" | "history" | "collections" | "playlists" | "nowplaying" | "settings" | "extensions" | plugin:${string}`. The unified Library is `"search"` (rendered by `SearchView`); `"artists"`, `"albums"`, `"tags"` are detail-page views reached by selecting an entity from the Library. Simple confirmation/error/loading modals are presentational leaf components in `components/modals/` (`ConfirmModals.tsx`) — App.tsx owns their state and passes primitive props + callbacks; the components hold no app state. The native context-menu spec building lives in `contextMenu/buildContextMenuSpecs.tsx` — a pure `buildContextMenuSpecs(target, deps)` that returns the `MenuItemSpec[]` (or `null`) per `ContextMenuTarget` kind; App.tsx keeps a thin `buildAndShowNativeMenu` wrapper that owns `setContextMenu` + `showNativeMenu`.
- **App.css** — All styles. CSS Grid layout, CSS custom properties for skinning, 7-level type scale (`--fs-2xs` through `--fs-2xl`). Shared motion easing tokens (`--ease-out-quart` / `--ease-out-quint` / `--ease-out-expo`) live in `base.css`, alongside a global `prefers-reduced-motion` guard. Shared keyframe animations: `fade-in`, `scale-in`, `glow-pulse`, `slide-text-in`, `view-fade` (per-view crossfade), `row-in` (list rows).
- **skinUtils.ts** — Skin validation, CSS generation, customCSS sanitization.
- **telemetry.ts** — Anonymous usage analytics wrapper. `track(event, props?)` fire-and-forgets to the self-hosted Aptabase backend by invoking the `plugin:aptabase|track_event` Rust command directly (the `@aptabase/tauri` npm package is Tauri v1 and incompatible — do not add it). Gated by `setTelemetryEnabled(bool)`, which App.tsx mirrors from the `telemetryEnabled` store flag (default on / opt-out) on startup and on the Settings toggle. Props must stay anonymous (enum/count only — never titles, paths, or PII). See `deploy/aptabase/` and backend `telemetry.rs`.
- **utils/errorKind.ts** — `classifyErrorKind(error)` buckets any error into a **closed** enum (`network`, `timeout`, `not_found`, `permission`, `auth`, `rate_limit`, `server`, `format`, `disk_full`, `cancelled`, `dependency`, `parse`, `unknown`) so the *kind* of failure can ride along on telemetry without ever sending the message. Every error event carries `error_kind`; the classifier is unit-tested to never echo its input. Also exports `errorText(unknown)` for readable text (report buffer only — never telemetry).
- **utils/errorLog.ts** — Always-on in-memory ring buffer (50 entries) of uncaught frontend errors, fed by App.tsx's `window` error / `unhandledrejection` handlers. Exists because the backend log file is OFF by default, so for most users the errors worth reporting are written nowhere. Memory-only, never persisted, never transmitted. Inspect via `window.__appErrors`.
- **utils/diagnosticReport.ts** — Builds the "Report a problem" bundle: `buildDiagnosticReport(input)` (pure markdown formatter, unit-tested), `collectDiagnosticReport(sources)` (gathers backend facts via `collect_diagnostics` + track/collection counts), `scrubPaths()` (home dir → `~`, always applied), `issueUrl()`. **Nothing is transmitted from here** — the user reviews the report and copies it. That review step is why the bundle may carry log lines and library metadata that telemetry never could; see `telemetry.ts` for that separate, anonymous-only contract.
- **types.ts** — Core TypeScript types (Track, QueueTrack, Artist, Album, Tag, etc.). `Track` is the full library type with DB IDs; `QueueTrack` is the ID-less metadata type used by queue/playback/playlists.
- **types/skin.ts** — Skin system types (SkinJson, SkinInfo, SkinColors, GallerySkinEntry).
- **types/plugin.ts** — Plugin types (PluginManifest, PluginState, PluginContextMenuTarget, PluginViewData, ViboplrPluginAPI).
- **types/informationTypes.ts** — InfoEntity, InfoFetchResult, InfoTypeDeclaration, DisplayKind.
- **skins/** — Built-in skin JSON files (8 skins) and index.

## Components (src/components/)

- **CaptionBar.tsx** — Custom caption bar: window controls, brand logo, `CentralSearchDropdown`, mini player button. Full-width drag region (`data-tauri-drag-region`).
- **Sidebar.tsx** — Navigation sidebar with animated active indicator. Items: Home (Cmd+0), Library (Cmd+1), History (Cmd+2), Now Playing (Cmd+3), Playlists, plugin sidebar items. Bottom: Collections (with sync-error badge, from `collectionAlert()` — see `ui.md`), Extensions (with update count badge), Settings (with update badge). Collapsible via Cmd+B. The Now Playing icon is playback-aware: `SpinningDisc` for audio, `FilmStrip` for video, both frozen when paused.
- **TrackList.tsx** — Table/list/tile view for tracks with column sorting, multi-selection (Click, Cmd+Click, Shift+Click), and drag-to-queue.
- **NowPlayingBar.tsx** — Footer playback controls. Full mode: seek bar (waveform or segmented), track info with like/dislike, transport controls, queue mode, auto-continue, volume. Mini mode: compact draggable bar with art, title/artist, play controls. Rank badges for top-100 tracks. Scrobble indicator checkmark. Album art resolved async via `currentTrack` effect in `App.tsx` — same priority chain as queue (see `queue.md` "Image Resolution" section). The **full bar** shows a static, clickable Artist · Album line under the title; the dynamic, cycling **Now Playing info** section (rendered by `NowPlayingInfoCycler`) is **mini-player only**.
- **NowPlayingInfoCycler.tsx** — Presentational cycling line for the Now Playing info section, used only by the mini player. Auto-rotates the host-resolved enabled items (5s) with the shared `slide-text-enter` animation; one item → static. Always rendered in `plain` mode (text only — no clickable links/rank badges). Items come from `useNowPlayingInfo`; which items show, in what order, and for how long is configured in Settings → Playback (`NowPlayingInfoSettings.tsx`) — see `ui.md` "Now Playing Bar" and `plugins.md` "api.nowPlayingInfo".
- **NowPlayingInfoSettings.tsx** — Settings → Playback control for that section: one row per registered item with drag-to-reorder (priority) and one select carrying both on/off and dwell (`Off` + the ToP presets — `parseDwellValue` maps the value back to `number | null`), plus "Reset to defaults". Reuses the `.provider-v*` list markup/CSS and `utils/rowDrag.ts` (`startRowDrag` + the pure `reorderList`), which is shared with the Settings → Providers priority lists.
- **contextMenu/buildQueueHeaderMenuSpecs.ts** — Pure builder for the queue header's `⋯` native menu (`buildQueueHeaderMenuSpecs(deps)` → `MenuItemSpec[]`); `QueuePanel` keeps a thin wrapper owning the anchor rect + `showNativeMenu`. Extracted for the same reason as `buildContextMenuSpecs`: a native menu has no DOM, so every item behind it — including **Clear playlist**, the only route to clearing the queue — is otherwise unassertable. New queue-level actions go here, not as extra header buttons.
- **QueuePanel.tsx** — Right sidebar playlist panel. Drag-and-drop reorder, multi-select, duplicate detection banner (add all / add new / cancel with auto-approve countdown). Resizable width. Collapsed mode (40px strip).
- **HistoryView.tsx** — Tabbed history: All Time, Last 30 Days, Recent (relative timestamps), Artists. Ghost entry reconnection on double-click. Exposes `reload()` via imperative handle.
- **CollectionsView.tsx** — Collection management with kind badges, resync, edit (name, enabled, auto-update, frequency).
- **PlaylistsView.tsx** — Saved playlists grid/detail.
- **ArtistDetail.tsx** — Circular avatar, like/hate, collapsible sections (Top Songs, About, Albums, Similar Artists), horizontal album scroll. Section toggles persisted as `artistSections`.
- **TrackDetailView.tsx** — Hero header with album art, quality stats (format, bitrate, sample rate, file size), Last.fm stats, inline tags. Sections: Scrobble History, Similar Tracks, Community Tags, Lyrics (LyricsPanel), Song Explanation (Genius). Section toggles persisted as `trackSections`.
- **InformationSections.tsx** — Tab-based interface for plugin-provided info. Drag-and-drop reorderable tabs, lazy-loaded with caching.
- **PluginViewRenderer.tsx** — Renders PluginViewData types: track-list, card-grid, track-row-list, text, stats-grid, buttons, toggles, tabs, layouts, search inputs, progress bars, settings rows. Threads a `playing` flag down to every node for kinds that need live transport.
- **Breadcrumb.tsx** — View title with play/queue all actions and `ViewModeToggle`.
- **ViewSearchBar.tsx** — Per-view search input with arrow key list navigation.
- **CentralSearchDropdown.tsx** — Global search (Cmd+K) with grouped results (Artists, Albums, Tracks), dynamic slot allocation (~7 items), 200ms debounce. Enter plays, Cmd+Enter enqueues. The library half comes from `search_all`; **plugin catalogs are queried on demand only** and render as `pluginSections` after the library rows (see `plugins.md` "Global Search"). A query with no library match renders a `showNoMatches` block instead of the empty box it used to — it offers the plugin sidebar views (`pluginViews` / `onOpenPluginView`) only when no *search provider* exists, since a searchable provider is strictly better than sending the user off to retype. Display-only rows (the no-match block, and a section's spinner / no-results / error rows) carry no item index and the arrow keys must never land on them.
- **utils/centralSearchPlugins.ts** — `buildPluginSearchSections(providers, states, baseIndex)`: lays out the plugin half of the dropdown, returning the rendered rows **and** the keyboard-selectable item list from one walk so the two can't disagree about an index. Also `ProviderRunState` (`idle` = offered but not asked) and `providerKeyOf`. Unit-tested for index contiguity across mixed provider states — that's the bug this shape exists to prevent.
- **ContextMenu.tsx** — Right-click context menu. Smart clamped positioning. Provider favicons.
- **ViewModeToggle.tsx** — Basic/list/tiles toggle.
- **WaveformSeekBar.tsx** — Waveform visualization via Web Audio API (RMS amplitude, max-RMS normalization). `useWaveform` computes a **width-independent** source peak array at one bucket per second (last bucket = remainder) and caches it as `{app_dir}/waveforms/{md5(key)}.json` where `key` is `v3::artist::title::duration` (bump the prefix when bucketing/normalization changes to invalidate old caches). `WaveformSeekBar` renders it by measuring its own canvas width and downsampling (average) to `min(peaks.length, maxBars)` bars, packing as many bars as fit without any *painted* bar dropping below `MIN_BAR_WIDTH_PX` (3px; `maxBars = floor(w·(1−BAR_GAP_RATIO) / MIN_BAR_WIDTH_PX)`, so the gap is accounted for). It re-fits via a `ResizeObserver` on the canvas (any layout-driven width change, not just window resizes), so the same cached array adapts to both the now-playing bar and the fullscreen bar. Draws a SoundCloud-style mirror (bright upper lobe + dim reflection below `AXIS_RATIO`).
- **LyricsPanel.tsx** — Synced lyrics with timed highlighting and auto-scroll (5s pause on manual scroll), plain lyrics display, edit mode with kind selector, provider badges.
- **AlbumCardArt.tsx, ArtistCardArt.tsx, TagCardArt.tsx** — Entity card images with lazy loading via IntersectionObserver.
- **AddServerModal.tsx, EditCollectionModal.tsx** — Collection modals.
- **TrackPropertiesModal.tsx** — Tabbed modal (Info, Tags, Similar, Artist, Album).
- **FullscreenControls.tsx** — **The** fullscreen control bar, shared by both fullscreen surfaces: seek (storyboard filmstrip for video, waveform for audio), art, like/dislike, title with artist/album links, transport, queue mode, randomize, auto-continue, volume, subtitles, queue toggle, exit. App.tsx builds one `fullscreenControlsProps` object and renders it twice — inside `.video-container` and inside `AudioFullscreen` — overriding only `onToggleFullscreen`, since each surface exits itself. Do not fork it: "the controls are the same in every fullscreen" is the contract, and two 40-prop call sites would drift on the first edit. The `active` prop declares "this is a live fullscreen even though there is no DOM `:fullscreen` element", and the idle auto-hide + cursor-hiding are gated on it. **Two** surfaces need it, both window-fullscreen: the audio overlay *and* native mpv video (`.video-container--native-fs`). Only the browser engine's video fullscreen is a real DOM `:fullscreen`. Miss it and the controls never hide — which is what happened on the mpv engine, the default.
- **NowPlayingView.tsx** — Lean-back current-track view (the `nowplaying` main view). Audio: blurred-art backdrop + album art + centered lyrics (no up-next panel — the Queue Panel owns upcoming tracks). Video: shared `<video>` repositioned to fill the column (theater mode, `.video-container--theater`) with a `VideoAmbientOverlay`. Lyrics via `useLyrics`, and collapsible: one `np-audio--nolyrics` flag covers both "this track has none" and "the user collapsed them" (persisted as `nowPlayingLyricsHidden`), and a visualizer in the art column grows to take the freed stage. The lyrics column is **unmounted** when collapsed, not hidden — the synced panel runs a position-driven auto-scroll that would keep ticking behind `display: none`. Slot sizing goes through the `--np-vis-size` custom property rather than per-case rules on the slot, because the two-class `--nolyrics` selectors would otherwise out-specify the narrow-window media query and hand a small window the large size. Art resolution is the pure `utils/nowPlayingArt.ts` → `resolveNowPlayingArt` (unit-tested), which reports **`pending`** as well as the path: art decides the whole surface regime (blurred backdrop + always-light `--hero-text-*` vs `--noart` skin gradient + skin text), so an in-flight lookup read as "no art" committed to the wrong regime and inverted every line of text a frame later on a light skin. While `pending`, the view holds the art regime over the `--np-backdrop-base` dark layer (`base.css` — the light text needs a dark base before there is anything to blur) and the art column shows an empty frame rather than the initials fallback. See `ui.md` "Now Playing View".
- **VideoAmbientOverlay.tsx** — Auto-hiding layers over the theater-mode video in NowPlayingView: up-next chip, title/artist intro, subtitle toggle + fullscreen button. No glow/tint layer — the video sits on the plain `--video-bg` letterbox (see `ui.md` "Now Playing View"). Idle-timer visibility mirrors FullscreenControls. Pure helpers in `utils/videoOverlay.ts`.
- **FilmStrip.tsx** — Animated film-strip icon (video counterpart to `SpinningDisc`; sprocket holes scroll while playing) used by the playback-aware sidebar Now Playing icon and the track art fallback.
- **HomeView.tsx, HomeHero.tsx, HomeShelf.tsx** — Home landing surface: `HomeView` composes the page and owns the inline shelf-visibility popover; radio-station hero carousel (`HomeHero`); horizontal shelves (`HomeShelf`, with the `resolveImagePath` helper). State owned by `useHome.ts`. See `ui.md` "Home View".
- **SearchView.tsx** — The unified Library view (the `search` view): tabbed Tracks/Artists/Albums/Tags, empty query shows the full library. With no query, an empty result means an empty *library*, not a missed search — the copy says so, and `hasPluginViews` splits it further: a streaming-only setup has no folder to add and its music lives behind the plugin views, so "add a folder or server" must not read as the only way forward.
- **ExtensionsView.tsx** — Plugin/extension management (install from gallery, enable/disable, updates). The plugin detail pane also carries a **Contributions** section: one `ds-toggle` row per context-menu item / sidebar view / search provider the plugin registered, so a user can trim a plugin they want to keep enabled. Rows come from `plugins.contributions`; see `plugins.md` "Per-Contribution Visibility".
- **SettingsPanel.tsx** — Settings UI, four tabs: **General** (app/update/telemetry, Window, Library, History), **Playback** (engine + engine component, exclusive audio, crossfade, **playback speed**, prefer video, ReplayGain, Now playing info list), **Providers** (priority ordering), **Debug**. Also: dependencies, skins, profiles. Accepts a one-shot `scrollToId` / `onScrolledToId` pair so other surfaces can deep-link to a section (used by the mini player's "Now playing info…" menu item); the panel resolves the owning tab itself via the `SECTION_TABS` map — callers pass only the element id, so moving a section between tabs never breaks a caller. Keep `SECTION_TABS` in step with the deep-linkable `id="…"` attributes.
- **DetailHero.tsx, DetailHeroBackground.tsx, DetailHeroEffect.tsx** — Shared native detail hero (multi-image crossfade background, FX looks, square/circle art, play/enqueue + overflow). Used by all detail pages and the plugin `detail-header` display kind. Images via `useDetailHeroImages.ts`.
- **AlbumDetail.tsx, TagDetail.tsx** — Detail-page wrappers composing the hero + track list + information sections for albums/tags. `AlbumDetail` renders the 240x240 cover, title, clickable artist, year, count, play all, and like/hate; album section toggles persisted as `albumSections`.
- **DownloadModal.tsx** — Unified download flow (provider-chain resolution, interactive manual search, progress). See conventions.md "Download Track".
- **BulkEditModal.tsx** — Multi-track tag/metadata bulk editor (`bulk_update_tracks`). Tag suggestions fold in Last.fm community tags via `useCommunityTags` + `appendCommunityTags` (artist-level tags only for multi-track selections).
- **LikeDislikeButtons.tsx** — Shared like (heart) / dislike (X) button pair, reused across surfaces.
- **BufferingChip.tsx** — "Buffering…" indicator for a stalled network stream, shared by `NowPlayingBar` and `FullscreenControls`. Renders **absolutely, over** the seek track (both hosts are `position: relative`) rather than taking layout — a chip that reflows would shove the waveform sideways on every hiccup, and a bar that jumps when the stream stutters is worse than no indicator. Deliberately not a toast: toasts dismiss after 4.5s and a stall can outlast that (same reasoning as the queue's backfill row). The number is **seconds of audio recovered** (`formatReadahead`), not a percentage — a percentage sat at a static 0 through real stalls; readahead climbs, which is what answers "is this recovering?". Degrades to the bare word when no engine supplied a figure.
- **SegmentedSeekBar.tsx** — Segmented (non-waveform) seek bar variant for the Now Playing bar (shown when no waveform exists, e.g. remote/subsonic tracks). Blocks are laid out **proportional to time** — one per second, or per minute when seconds pack below `MIN_SEG_WIDTH` (3px) — so a block's left edge lands on that exact second/minute mark under the linear seek-click map, and the final block is the leftover remainder (narrower). Uses the same `AXIS_RATIO` mirror (bright upper lobe + dim reflection) as `WaveformSeekBar` so the fallback reads consistently.
- **AutoContinuePopover.tsx, EqPopover.tsx** — Popover controls for auto-continue strategy weights and the equalizer.
- **MiniSearchPanel.tsx** — Quick-search panel for mini mode (opened by typing when no input is focused).
- **TitleLineInfo.tsx** — Renders the `title_line` information display kind inline in detail headers.
- **VideoFilmstrip.tsx, VideoFrameCard.tsx, VideoRowThumb.tsx** — Video frame thumbnails (filmstrip + per-row/card art) backed by `useVideoFrames.ts` / `video_frames.rs`.
- **SpinningDisc.tsx** — Animated spinning-disc icon (audio counterpart to `FilmStrip`).
- **AutocompleteInput.tsx, PromptModal.tsx, ConfirmModal.tsx, DeletePlaylistModal.tsx, SavePlaylistModal.tsx, DependencyModal.tsx, MixtapeExportModal.tsx, MixtapePreviewModal.tsx, PlaybackErrorModal.tsx, PublishSourceModal.tsx, HeroOverflowMenu.tsx** — Supporting inputs/modals/menus (see conventions.md for native-menu and modal-dismiss rules).
- **OnboardingWizard.tsx** — First-run setup wizard shown once per fresh profile (after restore, gated on the `onboardingComplete` store flag). Data-driven step machine (pure helpers in `onboardingSteps.ts`): usage-profile picker (Normal / Video / Streaming / Server — drives plugin pre-selection via the gallery's optional per-entry `profiles` list, hides the Last.fm step for Video, reorders/auto-expands Subsonic for Server, adds a Track-video-history toggle for Video; choice persisted as `onboardingProfile` and preselected on re-runs), then welcome + skin picker (live preview via `useSkins.previewSkin`/`applySkin`), music sources (local folder / Subsonic via the shared `SubsonicServerForm` / manifest URL — `add_collection` auto-starts the scan; the step's **header** comes from the profile's `musicTitle`, which marks it optional for `streaming`), recommended gallery plugins (installs, **enables** via `togglePlugin`, then **awaits** `onRecheckDeps` inside the busy window so the companion-binaries step is decided before Continue re-enables — fire-and-forget raced, and a quick Continue skipped the binary offer for good), companion binaries (shown only when an enabled plugin needs a missing dep, e.g. yt-dlp; with **no** collections added, a *required* missing binary switches the intro from "optional" to naming the consequence — plugins are then the only source, so nothing plays without it), Last.fm (embeds the plugin's settings panel via `PluginViewRenderer` + `getViewData`), playback prefs (crossfade + auto-continue), finish (shortcuts cheat sheet + live scan progress). Every step is skippable; closing marks `onboardingComplete` (+ legacy `pluginRecommendationsShown`). Existing profiles (collections present or legacy flag set) are marked complete silently. Re-runnable via Settings → General → "Setup Wizard". Replaced the old standalone `FirstRunPluginModal`.
- **ReportProblemModal.tsx** — "Report a problem": assembles the diagnostic bundle (`utils/diagnosticReport.ts`), shows it in full in an editable read-only textarea, and offers Copy / "Copy & open GitHub issue". Reached from **Settings → Debug → Problems** and from `PlaybackErrorModal` (which prefills a context block with the failure and pauses its auto-skip countdown once a report is opened). Showing the report before it goes anywhere is the consent model — never add an auto-submit path.
- **HelpLink.tsx** — Small "?" icon that opens a topic on the site's help page (`LINKS.helpPage` + `#<anchor>` → `docs/help.html`). Placed next to settings labels that need more explanation than their one-line description. Anchors on that page are add-only (see site.md); a new `HelpLink` requires the matching `help.html` section in the same change.
- **VisualizerSlot.tsx** — Hosts one plugin visualizer. Also exports **`clampRate`** (0.25–4; zero/negative/non-finite → 1), which guards `actions.setRate`: rate is the first contract write that is audible, subtle and sticky, so it is the first that could really cost a user their music. attaches the shadow root, adopts the reduced-motion base sheet (the global CSS guard can't cross a shadow boundary), builds `PluginVisualizerHost`, and **owns the animation loop** so an off-screen or throwing visualizer can be stopped. That loop is **capped at `VISUALIZER_TARGET_FPS` (60)** via the pure, unit-tested `shouldRenderFrame`. The cap is a **portability ceiling, not a macOS saving**: WKWebView drives rAF at 60Hz even on a 120Hz ProMotion panel (instrumented in the running app), so it skips nothing here — it bounds webviews that do follow a high-refresh display. The gate draws once 75% of the budget has elapsed rather than strictly at it, because a strict deadline drops any display whose interval doesn't divide the budget to *half* the target (144Hz → 48fps). It also **ref-counts the module-level audio bus** (`acquireAudioBus` / `releaseAudioBus`) and suspends it 2s after the last slot unmounts — a running `AudioContext` wakes its render thread every ~2.7ms forever otherwise, and the bus outlives every slot. Suspended, not closed (a closed context can't be reused, and contexts are capped); still only *built* on the first `host.audio` read. Slot resolution/picker specs are pure in `utils/visualizerSlots.ts`. Two slots are wired: **`nowplaying`** (replaces the art column) and **`fullscreen`** (see `AudioFullscreen.tsx`); selection persists as `visualizerSlots`. App.tsx drops the `nowplaying` instance while the fullscreen one is up — the opaque overlay hides it but the slot's `IntersectionObserver` still counts it as on-screen, so it would otherwise paint a second copy of the same visualizer behind the first. The reference consumer is the external `viboplr-vinyl-deck` plugin; no visualizer ships in this repo. See `plugins.md` "Visualizers".
- **AudioFullscreen.tsx** — Fullscreen for a **non-video** track, mounted at the app root (not inside the Now Playing view) so it pins over everything without inheriting the grid. A dumb container: `stage` (the `fullscreen` visualizer slot when one is selected, else the album art, else `TrackArtFallback`) plus `controls` (the shared `FullscreenControls`). **Availability is not gated on having a visualizer** — a visualizer is only what fills the screen when one exists, and gating on it made fullscreen appear and disappear with a plugin setting. Uses **window** fullscreen (`getCurrentWindow().setFullscreen`) plus a pinned overlay, the same choice the native video path made — `requestFullscreen` is activation-consuming in WKWebView and on macOS moves the webview to its own space. `z-index: 999` matches `.video-container--native-fs`, leaving modals and toasts above it. `.audio-fs .fs-controls { display: block }` is required: the bar hides itself outside a DOM `:fullscreen` ancestor. Entered via **Cmd/Ctrl+F**, the Now Playing fullscreen button, or its ⋯ menu; exited via the bar's exit button or **Escape**.
  - **Why not one surface for all three:** the video path fullscreens `.video-container`, and the shared `<video>` must never be remounted or moved out of it (see `ui.md`). So the *container* differs and the *controls* are what's shared. From the user's side there is one fullscreen with one control bar.
- **utils/visualizerSlots.ts** — slot resolution plus `buildVisualizerMenuSpecs` (the native picker) and `ARTWORK_VISUALIZER_NAME`. The picker's first entry is **"Artwork"**, a peer of the plugin entries rather than a "None" above a separator: an unselected slot has never rendered nothing, it renders the track's album/artist image, so "None" named the mechanism and misnamed the result. It is still **persisted as `null`** — the rename is presentation-only, so no stored `visualizerSlots` needs migrating and nothing has to teach `resolveSlot` / `VisualizerSlot` about a visualizer with no plugin behind it. With no plugin installed the picker shows Artwork (checked) plus a disabled "No visualizer plugins installed" hint, instead of only the hint. There is **no** `contextMenu/buildNowPlayingMenuSpecs.ts` — the Now Playing view's menu was replaced by buttons (see `ui.md` "Now Playing View") and the builder and its test were deleted.
- **StatusBar.tsx, Icons.tsx, WindowControls.tsx** — Utility components.

## Hooks (src/hooks/)

- **usePlayback.ts** — Dual A/B audio element architecture for gapless/crossfade. Crossfade via setInterval volume ramp. Video tracks don't crossfade. `currentTrack` is `QueueTrack | null`. Also owns the **native mpv session branches**: when the full build's engine is selected (`useNativeEngineRef`), eligible audio routes through `src/playback/nativeEngine.ts` instead of the elements — `nativeSessionRef` marks the session, `engine-*` events stand in for element events, and any `engine-error` blocklists the key + replays the same track at the same position on the browser engine (per-track fallback). The scrobble/preload/crossfade decision logic is extracted to `src/playback/progressMachine.ts` (pure, unit-tested) and driven by BOTH `timeupdate` and `engine-position`. Also owns **`playbackRate`** (exposed as `playbackRate` / `setPlaybackRate`), applied to both audio elements *and* the preloaded one *and* mirrored to mpv — an element left at 1.0 would snap back to normal at the gapless hand-off. `applyRateTo()` sets `preservesPitch = false` (and the webkit-prefixed name), which is the one line that makes this a deck rather than a speed-reader: pitch follows tempo, as a turntable resamples. Deliberately **not persisted** — a rate silently restored at 1.35x reads as "my library got transposed", so every launch starts at 1x. Also owns **`buffer`** (`PlaybackBuffer | null`, see `src/playback/bufferState.ts`): fed by the `engine-buffer` subscription on the native path and by `onMediaProgress` / `onMediaWaiting` / `onMediaPlaying` (wired onto both `<audio>` elements and the `<video>` in App.tsx) on the browser path. Two rules keep the engines saying the same thing: the browser buffered-range read is skipped for `isLocalTrack` sources (matching the engine's `demuxer-via-network` gate), and stall detection is armed only after the first `playing` event (`browserPlaybackStartedRef`) — a media element fires `waiting` during its *initial* load and mpv's `paused-for-cache` does not, so without the gate every browser-engine track start would flash "Buffering…" at an ordinary load. `clearStreamReadouts()` resets `icyTitle` + `buffer` + that ref together at every session boundary (explicit play, stop, gapless hand-off, engine track change, engine error) — one call site per boundary, not one per readout.
- **src/playback/nativeEngine.ts** — Typed invoke/listen bridge to the mpv engine (`engine_*` commands, `engine-*` events). Capability probed via `engine_capabilities` and cached; `refreshEngineCapabilities()` drops the cache and re-probes (used after an engine-component install — backend load failures aren't cached, so a fresh install unlocks live, no restart). All control methods no-op when incapable. EQ/ReplayGain/**speed** settings are mirrored to it from usePlayback's existing effects (cached backend-side until the engine runs). `setSpeed` maps to mpv's `speed` on BOTH decks — the idle one is what gapless preloads into. mpv's `audio-pitch-correction` is turned **off** at init so speed shifts pitch too.
- **src/playback/bufferState.ts** — `PlaybackBuffer` (`stalled` / `readaheadSecs` / `bufferedToSecs`) plus the pure `bufferedEndAt` / `bufferedFraction` / `formatReadahead` / `applyBuffer`. Where the two engines' buffer reporting is normalized: mpv sends the `engine-buffer` event, and the browser engine yields the same two numbers from `HTMLMediaElement.buffered` — the covering range's end is the buffered edge, `end - currentTime` is the readahead — plus `waiting`/`playing` for the stall flag. There is deliberately **no refill percentage** (see backend.md: mpv's reads a near-constant 0 through a real stall). **`null` means unknown, not empty** — most local files report nothing, and rendering that as "0% buffered" would dim the whole seek bar for a file already on disk; every consumer treats null as "draw no buffered edge". `bufferedEndAt` returns only the range *covering* the play position: data past a gap can't be played through, so drawing out to it would promise a smooth ride the element can't deliver. `applyBuffer` returns the previous object identity when nothing moved, because the browser `progress` handler fires several times a second and a fresh object each time re-renders the whole now-playing bar for no visible change.
- **src/playback/progressMachine.ts** — `driveProgressMachine()`: given position/duration/arming state, decides prefetch / preload (20s lead, 45s when the next track needs stream resolution) / crossfade-start. Single source of truth for both engines. **Every threshold is in track seconds but budgets wall-clock time**, so they are all scaled by `rate` — at 1.35x an unscaled 45s stream-resolve lead is only 33 real seconds, quietly eating a quarter of the window yt-dlp was given, and the failure looks like a gap between tracks rather than a rate bug.
- **useQueue.ts** — Queue management using `QueueTrack[]`. `playTracks()` / `enqueueTracks()` / `playNextInQueue()`. Duplicate detection via `findDuplicates()`. Image stamping uses name-based lookup (no DB IDs).
- **useLibrary.ts** — Library data queries, column configuration, view modes, sort/filter state.
- **usePlugins.ts** — Plugin discovery, loading (`new Function("api", code)`), activation, event dispatch, context menu dispatch, view data management, enable/disable, reload. Also applies the **per-contribution visibility filter** (`utils/pluginContributions.ts`) to `menuItems` / `sidebarItems` / `searchProviders` before returning them, and exposes `contributions` / `contributionVisibility` / `setContributionEnabled` for the Extensions → Contributions UI. Filtering here — not at call sites — is what makes it reach native menus. `sidebarItemsUnfiltered` is the pre-filter list, for consumers asking "does this user have plugin views at all?" rather than "what goes in the nav" (App.tsx `pluginViewList`). See `plugins.md` "Per-Contribution Visibility".
- **useEventListeners.ts** — Tauri backend event subscriptions for all `listen<T>()` events.
- **useImageCache.ts** — Entity image caching and on-demand fetching with dedup guards. `getImage` returns `null` for both "no image" and "the lookup is still in flight"; `isResolved(name, artistName?)` distinguishes them (a settled miss is stored as an explicit `null`, so key presence is the signal). Consumers that merely show an `<img>` late don't care — one that changes *layout or text regime* on the answer must use it, or it commits to "no image" and flips a frame later (see `NowPlayingView`).
- **useAutoContinue.ts** — Auto-continue with 5 weighted strategies: Random (40%), Same Artist (20%), Same Tag (20%), Most Played (10%), Liked (10%). "Same format" filter option.
- **useMiniMode.ts** — Mini player (40px height, 280-550px auto-width). Always-on-top, no decorations. macOS transparent background via Cocoa APIs.
- **useVideoLayout.ts** — Resizable video splitter / dock layout (default 300px, min 150px track list; dock sides top/bottom/left/right).
- **useWaveform.ts** — Waveform data caching and computation.
- **useGlobalShortcuts.ts** — OS-level media keys (MediaPlayPause/Next/Previous/Stop) via Tauri's global-shortcut plugin.
- **useInAppKeyboardShortcuts.ts** — In-window keyboard shortcuts (the `window` keydown handler; see "Keyboard Shortcuts" below). Takes a single `deps` object refreshed into a ref each render, so its one installed listener never reads stale closures. App.tsx owns the deps; the hook owns the dispatch.
- **useViewSearchState.ts** — Per-view independent search state, persists across view switches.
- **useCentralSearch.ts** — Global search. Library results via `search_all` (200ms debounce), plus per-plugin-catalog run state (`providerStates`, keyed `pluginId:providerId`) for the on-demand plugin search. A query change bumps `queryGenRef` and clears those states, so a provider that's still running — legitimately for tens of seconds — is discarded on return rather than shown against a query the user has moved on from. `items` is library rows then plugin rows in one flat list (one highlight index for the whole dropdown); `pluginSections` is the render side of the same walk. Activating a `plugin-run` item keeps the dropdown open, since its results land in place.
- **useNavigationHistory.ts** — Single-direction back navigation (history stack only, no forward) with per-view search query persistence and scroll restoration. History is pushed **only on detail-page entry** — the `onBeforeNavigate` hook fired by `useLibrary`'s `handle*Click` / `navigateTo*ByName` paths (plus plugin navigate-to-entity). Top-level view switches (sidebar, Cmd+1/2/3) do **not** push. Because a detail page is only reachable through a push point, the top of the stack is always the page's immediate origin, so the first back is always correct. The only back affordance is the detail-page back button (`DetailHero` `onBack`, gated on `canGoBack`); there is no caption-bar back/forward UI and no global keyboard/mouse nav shortcuts.
- **useToasts.ts** — Transient user feedback via `notify()`; rendered by `Toasts.tsx`. Toasts auto-dismiss after 4.5s, so they are not a substitute for a persistent indicator (see the backfill "Filling in the rest…" row in `queue.md`).
- **useAppUpdater.ts** — App update checking and installation. `updateState.error` is a **persistent** `UpdateError` (`stage` / `message` / `detail` / `version`) held until the user retries or dismisses — a toast auto-dismisses in 4.5s and the failure users most need to see is the one from a background check they weren't watching, so the toast is an addition to it, never the only surface. Background check failures record the error but deliberately raise **no** toast (unprompted work shouldn't interrupt) and never overwrite an existing one. `humanizeUpdateError(raw, stage)` (pure, unit-tested in `__tests__/updateErrors.test.ts`) maps transport text to an actionable sentence; the raw text survives as `detail` for the diagnostic report only. Rendered by `SettingsPanel`'s `UpdateProgress` (percent + bar + MB, indeterminate when the server sends no Content-Length) and `UpdateErrorRow` (Try again / Download from viboplr.com / Report a problem / Dismiss). `updateBadgeFor(state)` (pure, unit-tested) derives the sidebar Settings dot — `error` outranks `available`, since Settings is the one place a user who doesn't yet know the update failed has no reason to open.
- **usePasteImage.ts** — Clipboard image paste handling for entity images.
- **useSkins.ts** — Skin management: load/apply/import/delete, gallery browsing, CSS injection via `<style id="viboplr-skin">`.
- **useLikeActions.ts** — Like/unlike for tracks (tri-state: -1/0/1), artists, albums, tags (binary: 0/1). Persists via `set_entity_like_state` (durable, ID-less `entity_likes` store) and propagates the new state to same-song copies in `currentTrack`/queue via the `sameSong()` predicate (key match, falling back to title+artist).
- **useContextMenuActions.ts** — Context menu action handlers (delete, show in folder, YouTube, queue ops, play/enqueue). Composes and re-exports `useQueueDragToInsert` (drag-to-queue) and `useDownloadActions` (download), and re-exports `playActions.startRadio`, so all of these stay reachable via `contextMenuActions.*` for existing consumers.
- **useQueueDragToInsert.ts** — Drag-to-queue: owns the `externalDropTarget` indicator and the raw mouse-listener / ghost-element DOM handshake; duplicate drops defer to the shared `pendingEnqueue` banner. (Split out of `useContextMenuActions`.)
- **useDownloadActions.ts** — Single + multi-track download actions; builds the `enqueue_download` payload via one `buildDownloadRequest()` helper and gates the re-download confirm on a `find_track_by_metadata` local-copy match. (Split out of `useContextMenuActions`.)
- **useCommunityTags.ts** — Fetches Last.fm community tags for a track via the `track_tags` information type (the same fetch `TrackDetailView` uses) so any tag-editing surface can suggest community tags, not just library tags. Returns merged track + artist tag names; `enabled` gates the network call (e.g. only while a popover is open), `includeTrackTags=false` returns artist-level tags only (used by multi-track bulk edit). Degrades to `[]` when the Last.fm plugin is absent. Consumed by `TagPopover` and `BulkEditModal`.
- **useLyrics.ts** — Fetches lyrics for the current track through the plugin info-type provider chain (scoped via the `include` filter on `useInformationTypes`). Used by `NowPlayingView`. Returns synced (LRC) or plain text.
- **useInformationTypes.ts** — Orchestrates plugin information-type fetching + caching for detail pages (cache decision logic, provider priority, in-flight dedup). Supports an `include` filter to scope fetches (used by `useLyrics`). See `plugins.md` "Information Sections".
- **useImageResolver.ts** — Bridge between the Rust image-download worker and JS plugin image providers (handles `image-resolve-request` events). See `plugins.md` "Image Provider Chain".
- **useHome.ts** — Home view state: built-in shelf resolvers, merged plugin shelves, radio-station selection (`pick_radio_seeds` + cover resolution), persisted snapshot + 24h refresh model. See `ui.md` "Home View".
- **useNowPlayingInfo.ts** — Resolves the cycling Now Playing info items for the current track: built-in resolvers (Artist · Album / Artist / Album / Plays · Rank / Source / Quality / Duration / Tags / Synced Lyrics / Plain Lyrics) merged with plugin items (`plugins.nowPlayingInfoItems`), fetched with a 5s timeout + generation guard. The two lyrics items resolve synchronously from cached lyrics (via `useLyrics`, fetched only when enabled) so the synced "current line" tracks playback position without re-running the async resolvers; they're merged back into the ordered list. Returns `availableItems` (context-menu checklist, already in the user's priority order) + `resolvedItems` (display, same order). Exports pure helpers `isNowPlayingItemSelected` / `formatPlays` / `formatSource` / `formatQuality` / `formatTags` / `nextCycleIndex` / `nowPlayingOrderedItems` / `setNowPlayingItemPriority` / `nowPlayingMenuLabel` (unit-tested); lyrics line helpers live in `utils/lyrics.ts`. Selection / ToP / order persisted by App.tsx as `nowPlayingInfoSelection` / `nowPlayingInfoPersistence` / `nowPlayingInfoOrder`.
- **useEntityDetail.ts** — Loads detail-page data (tracks/albums/sections) for a selected artist/album/tag.
- **useDetailHeroImages.ts** — Resolves the multi-image background set for `DetailHero`.
- **useArtistInfo.ts** — Artist metadata loading for the artist detail page.
- **usePlayActions.ts** — Shared play/enqueue action helpers routed through the canonical queue actions. Also owns `playWithBackfill` (play a known head now, append the async remainder — generation-guarded via `useQueue.appendToPlaySession`, head de-duped by the exported pure `dropPlayedHead`) and `startRadio` (which enriches the banner cover after playback starts). See conventions.md "Play With Backfill".
- **useCollectionActions.ts** — Collection CRUD/resync action handlers for `CollectionsView`.
- **useExtensions.ts** — Extension/plugin install, enable/disable, update checking for `ExtensionsView`. Three rules the "Update All" freeze came from, all load-bearing: (1) the `extension-update-installed` listener re-checks **only** for installs it didn't initiate (`selfInitiatedRef`) — re-checking per install made a batch quadratic, since each check is itself N network round-trips; (2) `performUpdate(id, { deferReload })` — a plugin reload rebuilds the *entire* plugin runtime, so a batch defers it and calls `onReloadAllPlugins()` **once** at the end; (3) reloads are **awaited** (`onReloadPlugin` returns `void | Promise<void>`) — fire-and-forget let N rebuilds overlap. `progress` (keyed by id, fed by `plugin-install-progress`) drives the byte readout in `PluginLoadingModal`. Failures that were `console.error`-only — uninstall, enable/disable, install-from-URL — now also reach the user via `onNotify` / the result modal.
- **useDependencies.ts** — External-binary dependency state (install/update/progress/check). See `backend.md` "External Binary Dependencies".
- **useEngineComponent.ts** — Install state + actions for the downloadable libmpv engine component (Settings > Playback): status, streamed `engine-component-progress`, install/uninstall invokes, and a `refreshEngineCapabilities()` re-probe on completion. See `backend.md` "Native mpv Engine".
- **useVideoFrames.ts** — Video frame thumbnail caching/extraction bridge (`video_frames.rs`).
- **useMiniSearch.ts** — Mini-mode quick-search state for `MiniSearchPanel`.

## Keyboard Shortcuts

No modifier (when not in text input): Space (play/pause), arrows (seek/volume).
Cmd/Ctrl: 0 (Home), 1 (Library), 2 (History), 3 (Now Playing), K (search), F (fullscreen), L (like), P (playlist panel), M (mute), Shift+M (mini), B (sidebar), Left/Right (prev/next track).
**Cmd/Ctrl+F** dispatches by track kind: video → the video/native-mpv fullscreen path; audio → the fullscreen visualizer, but only when one can fill the slot (otherwise the key falls through and does nothing, as before). The two never contend for the key.
Track list: a focusable ARIA listbox (`TrackList` — `role="listbox"`/`"option"` + `aria-activedescendant`). Tab to focus the list, then arrows / Home / End move the cursor (Shift+arrow extends selection), Space toggles selection, Enter plays, Shift+Enter enqueues. Cmd/Ctrl+A selects all, Delete removes (local), Escape clears selection.
Mini mode: any printable character (when no input is focused) opens the mini-player quick-search panel; Space/arrows remain player controls.

## State Persistence

UI state persisted via `tauri-plugin-store` to `app-state.json` in profile directory. Key states: current track + position, queue (track IDs + index + mode), volume, window geometry (full + mini separately), crossfade, auto-continue weights, view modes per entity, Extensions plugins layout (`pluginViewMode`: `"cards"` | `"list"`), per-contribution plugin visibility (`pluginContributionVisibility`), skin, sort/filter per entity, section toggles, Now Playing info selection / dwell / order (`nowPlayingInfoSelection`, `nowPlayingInfoPersistence`, `nowPlayingInfoOrder`), Now Playing lyrics collapsed (`nowPlayingLyricsHidden`), onboarding profile (`onboardingProfile`), anonymous-telemetry opt-out (`telemetryEnabled`, default on) + one-time install-reported guard (`telemetryInstallReported`). (Last.fm session/auto-import state lives in plugin storage, owned by the lastfm plugin — not the app store.) Saves debounced at 500ms. A `restoredRef` guard prevents overwriting persisted data with defaults on startup. On mount, App.tsx's restore effect reads the primary settings via `startup/readPersistedSettings.ts` (`readPersistedSettings(store)` returns a named object — no positional tuple) and then applies them to its hooks/setters. `view` and selected-entity state are intentionally NOT read/restored — startup always lands on Home.

## Scrobble Threshold

Play history / Last.fm scrobble recorded after 50% of duration or 4 minutes (whichever first). Tracks < 30 seconds are never recorded. Video tracks excluded unless "Track video history" is enabled.
