{
  "id": "mock-download",
  "name": "Mock Download",
  "version": "1.0.0",
  "author": "Viboplr",
  "description": "Mock download provider for testing. Returns fake search results and downloads free MP3 files.",
  "minAppVersion": "0.9.4",
  "debugOnly": true,
  "contributes": {
    "downloadProviders": [
      { "id": "mock-dl", "name": "Mock" }
    ],
    "assistant": {
      "instructions": "Mock provider for testing the assistant-tool path. search_catalog finds fake tracks by title/artist substring; get_state returns the failure-simulation knobs (set in Settings → Mock Download).",
      "tools": [
        {
          "name": "search_catalog",
          "description": "Search the fake 10-track catalog by title/artist substring",
          "inputSchema": { "type": "object", "properties": { "query": { "type": "string" } }, "required": ["query"] }
        },
        {
          "name": "get_state",
          "description": "The mock provider's current simulation settings (fail/error rates, delay)"
        }
      ]
    },
    "settingsPanel": {
      "id": "mock-download-settings",
      "label": "Mock Download",
      "order": 90
    }
  }
}
